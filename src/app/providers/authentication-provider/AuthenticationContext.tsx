import { login } from "@/features/auth/services/authService"

export const authProvider = ({children } : React.ReactNode) =>{


    const [ isAuthenticated,setU ]

    login
    sig


    return (



    )

}